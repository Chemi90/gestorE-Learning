spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: content
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
