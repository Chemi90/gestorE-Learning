spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: content
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
